{
  "name": "task-manager",
  "version": "1.0.0",
  "description": "this is task manager app",
  "main": "be/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd be && npm run dev\" "
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.1.2",
    "mongoose": "^8.13.2"
  }
}
